import VerticalPageContent from "@/components/VerticalPage";

export const metadata = { title: "Eventos" };

export default function EventosPage() {
  return <VerticalPageContent slug="eventos" />;
}
